<?xml version="1.0" encoding="UTF-8" ?>
<project name="Template - BootstrapBase" default="">
    <property file="build.properties"/>

    <target name="install" description="Copies Template - BootstrapBase to existing Joomla install.">
        <copy todir="${dest}/templates/bootstrapbase">
            <fileset dir="${src}" defaultexcludes="true">
                <include name="**"/>
                <exclude name="**/language/**"/>
                <exclude name="**/build.xml"/>
                <exclude name="**/build.properties"/>
            </fileset>
        </copy>
        <copy todir="${dest}/language">
            <fileset dir="${src}/language" defaultexcludes="true">
                <include name="**/**"/>
            </fileset>
        </copy>
    </target>

    <target name="package" description="Create an installable package.">
        <copy todir="${package.dest}tpl_bootstrapbase">
            <fileset dir="${src}" defaultexcludes="false">
                <exclude name="build*.xml"/>
                <exclude name="build.properties*"/>
            </fileset>
        </copy>

        <delete file="${package.dest}tpl_bootstrapbase.zip"/>

        <zip destfile="${package.dest}tpl_bootstrapbase.zip"
               basedir="${package.dest}tpl_bootstrapbase/"
               excludes="build*.xml"/>

        <delete dir="${package.dest}tpl_bootstrapbase"/>
    </target>
</project>